version: '3.8'
services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - JWT_SECRET=your_jwt_secret_key_here
      - GLM_API_KEY=7b8a15f57d2941a69fcce60f49f7c6ff.SiMrZjCdyOmdtzLr
    volumes:
      - ./database:/app/database

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - server
